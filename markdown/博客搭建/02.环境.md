02.环境

服务器采用的是腾讯云的轻量服务器，debian12系统。

#### docker 环境

安装命令

```shell
apt install docker docker-compose docker.io
```

#### git

安装命令

```shell
apt install git
```

注：由于国内服务器连github可能有问题，可以改host缓解（但还是经常不能用），若实在不能用可以考虑换个代码托管平台


